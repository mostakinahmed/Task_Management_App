<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form p-10">
        <form action="/create" method="post">
          <input
            class="block px-5 py-2 bg-zinc-800 text-white rounded-md mb-3 w-full"
            type="text"
            placeholder="Title here"
            name="title"
          />
          <textarea
            class="block px-5 py-2 bg-zinc-800 resize-none text-white rounded-md mb-3 w-full"
            placeholder="Write your task details"
            name="details"
          ></textarea>
          <input
            type="submit"
            value="Create Task"
            class="px-5 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-md"
          />
        </form>
      </div>

      <div class="task p-10 flex gap-3 flex-wrap">
        <% if (files.length > 0) { %> <% files.forEach(function(val) { %>
        <div class="w-72 px-3 py-4 bg-zinc-800 rounded-xl shadow-md">
          <h1 class="text-4xl font-bold text-center text-blue-500 mb-4">
            <%=val%>
          </h1>
          <a class="text-blue-500 inline-block mt-3 hover:underline" href="#">
            Read More.
          </a>
        </div>
        <% }) %> <% } else { %>
        <h3 class="text-zinc-600">No tasks yet.</h3>
        <% } %>
      </div>
    </div>
  </body>
</html>
