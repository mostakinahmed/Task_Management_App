<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mostakin</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
    />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form p-10">
        <form action="/create" method="post">
          <input
            class="block px-5 py-2 bg-zinc-800 text-white rounded-md mb-3 w-full"
            type="text"
            placeholder="Title here"
            name="title"
          />
          <textarea
            class="block px-5 py-2 bg-zinc-800 resize-none text-white rounded-md mb-3 w-full"
            placeholder="Write your task details"
            name="details"
          ></textarea>
          <input
            type="submit"
            value="Create Task"
            class="px-5 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-md"
          />
        </form>
      </div>
      <div class="task p-10 flex gap-5 flex-wrap">
        <% if (files.length > 0) { %> <% files.forEach(function(val) { %>
        <div class="w-72 px-5 py-6 bg-zinc-800 rounded-2xl shadow-lg">
          <h1
            class="text-2xl font-semibold text-center text-blue-400 mb-4 break-words"
          >
            <%= val %>
          </h1>

          <div
            class="flex justify-between items-center mt-[50px] text-sm text-blue-400"
          >
            <a class="hover:underline" href="/file/<%= val %>">Read More</a>

            <div class="flex gap-4">
              <a class="hover:text-blue-600" href="/edit/<%= val %>">
                <i class="fa-solid fa-pen-to-square"></i>
              </a>
              <a
                class="text-red-500 hover:text-red-700"
                href="/delete/<%=val%>"
              >
                <i class="fa-solid fa-trash"></i>
              </a>
            </div>
          </div>
        </div>
        <% }) %> <% } else { %>
        <p class="text-gray-400 text-center w-full">No files found.</p>
        <% } %>
      </div>
    </div>
  </body>
</html>
