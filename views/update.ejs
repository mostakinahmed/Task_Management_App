<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mostakin</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
    />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div
      class="main w-full min-h-screen bg-zinc-900 flex items-center justify-center"
    >
      <div class="form p-10 bg-zinc-800 rounded-lg shadow-lg w-full max-w-md">
        <form action="/update" method="post">
          <input
            class="block px-5 py-2 focus:outline-none bg-zinc-700 text-white rounded-md mb-3 w-full"
            type="text"
            value="<%= prevName %>"
            name="prevName"
          />
          <textarea
            class="block px-5 py-2 bg-zinc-700 resize-none text-white rounded-md mb-3 w-full"
            placeholder="Enter New File name"
            name="newName"
          ></textarea>
          <input
            type="submit"
            value="Update"
            class="px-5 py-2 bg-red-600 hover:bg-red-500 text-white rounded-md w-full"
          />
        </form>
      </div>
    </div>
  </body>
</html>
